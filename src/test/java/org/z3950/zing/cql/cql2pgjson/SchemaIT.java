package org.z3950.zing.cql.cql2pgjson;

public class SchemaIT extends SchemaTest {
  // Same tests from SchemaTest but run them against a jar file.
  // Integration tests (IT) have the resource files in the jar file,
  // unit tests have them in the classes and test-classes directory.
}
